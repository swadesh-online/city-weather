server:
  port: 8080

spring:
  application:
    name: gateway-api
  cloud:
    gateway:
      routes:
      - id: city-api
        uri: http://city-api/
        predicates:
        - Path=/city/**
      - id: weather-api
        uri: http://weather-api/
        predicates:
        - Path=/weather/**
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka

 